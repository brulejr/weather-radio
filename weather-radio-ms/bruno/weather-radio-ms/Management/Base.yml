info:
  name: Base
  type: http
  seq: 1

http:
  method: GET
  url: "{{weather_radio_ms_url}}/mgmt"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
