info:
  name: Get Prune History
  type: http
  seq: 3

http:
  method: GET
  url: "{{weather_radio_ms_url}}/api/admin/alerts/artifacts/prune/history"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
