info:
  name: Get Alerts - EXPIRED
  type: http
  seq: 4

http:
  method: GET
  url: "{{weather_radio_ms_url}}/api/alerts?state=EXPIRED"
  params:
    - name: state
      value: EXPIRED
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
