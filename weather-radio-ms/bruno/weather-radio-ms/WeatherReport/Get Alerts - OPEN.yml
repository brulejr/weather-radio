info:
  name: Get Alerts - OPEN
  type: http
  seq: 2

http:
  method: GET
  url: "{{weather_radio_ms_url}}/api/alerts?state=OPEN"
  params:
    - name: state
      value: OPEN
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
