info:
  name: Get Latest Transcript
  type: http
  seq: 4

http:
  method: GET
  url: "{{weather_radio_ms_url}}/api/weather/radio/latest-transcript"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
