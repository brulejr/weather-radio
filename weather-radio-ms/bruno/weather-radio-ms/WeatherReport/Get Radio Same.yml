info:
  name: Get Radio Same
  type: http
  seq: 3

http:
  method: GET
  url: "{{weather_radio_ms_url}}/api/weather/radio/latest-same"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
