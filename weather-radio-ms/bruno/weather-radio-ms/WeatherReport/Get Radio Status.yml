info:
  name: Get Radio Status
  type: http
  seq: 2

http:
  method: GET
  url: "{{weather_radio_ms_url}}/api/weather/radio/status"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
